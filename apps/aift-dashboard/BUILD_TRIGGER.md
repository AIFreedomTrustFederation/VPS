# Dashboard Build Trigger

This file exists to trigger the AIFT Dashboard Build workflow after workflow setup.

Last trigger purpose: verify that the dashboard can install dependencies and run `npm run build` in GitHub Actions before deploying to the VPS.

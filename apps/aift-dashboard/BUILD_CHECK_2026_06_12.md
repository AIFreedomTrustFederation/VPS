# Dashboard Build Check

This file intentionally lives inside the dashboard app folder so the dashboard build workflow runs.

Purpose: verify the latest dashboard foundation, build report workflow, and source setup documentation.
